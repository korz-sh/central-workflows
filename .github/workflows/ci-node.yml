name: CI Node (Reusable)

on:
  workflow_call:
    inputs:
      node-version:
        required: false
        type: string
        default: '20.x'
      service-name:
        required: true
        type: string
      package-manager:
        required: false
        type: string
        default: 'npm'
        description: 'npm or pnpm'
      pnpm-version:
        required: false
        type: string
        default: '9.1.4'
      coverage-glob:
        required: false
        type: string
        default: 'src/**/*.ts'
        description: 'Glob for --collectCoverageFrom (e.g. src/**/*.{ts,tsx})'
      run-type-check:
        required: false
        type: boolean
        default: false
      codecov-flag:
        required: false
        type: string
        default: ''
        # From node-pr-check.yml, which this workflow absorbs. The codecov
        # upload lived only there, so folding that file in without this input
        # would have dropped the upload silently.
        description: 'Flag for the codecov upload'

jobs:
  # This job is what node-pr-check.yml's `build` job already did: install, lint,
  # build. That file is deleted in this change -- its `test` job also duplicated
  # the `test` job below, so every PR installed dependencies four times across
  # four runners and ran the Jest suite twice.
  validate:
    runs-on: ${{ fromJSON(vars.KORZ_RUNNER_LABELS || '"ubuntu-latest"') }}
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        if: ${{ inputs.package-manager == 'pnpm' }}
        uses: pnpm/action-setup@v4
        with:
          version: ${{ inputs.pnpm-version }}

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node-version }}
          cache: ${{ inputs.package-manager }}

      - run: ${{ inputs.package-manager == 'pnpm' && 'pnpm install --frozen-lockfile' || 'npm install' }}
      - run: ${{ inputs.package-manager == 'pnpm' && 'pnpm run lint' || 'npm run lint' }}
      - if: ${{ inputs.run-type-check }}
        run: ${{ inputs.package-manager == 'pnpm' && 'pnpm run type-check' || 'npm run type-check' }}
      - run: ${{ inputs.package-manager == 'pnpm' && 'pnpm run build' || 'npm run build' }}

  test:
    runs-on: ${{ fromJSON(vars.KORZ_RUNNER_LABELS || '"ubuntu-latest"') }}
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        if: ${{ inputs.package-manager == 'pnpm' }}
        uses: pnpm/action-setup@v4
        with:
          version: ${{ inputs.pnpm-version }}

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node-version }}
          cache: ${{ inputs.package-manager }}

      - run: ${{ inputs.package-manager == 'pnpm' && 'pnpm install --frozen-lockfile' || 'npm install' }}
      - run: ${{ inputs.package-manager == 'pnpm' && format('pnpm test -- --coverage --collectCoverageFrom="{0}"', inputs.coverage-glob) || format('npm test -- --coverage --collectCoverageFrom="{0}"', inputs.coverage-glob) }}

      # Kept from node-pr-check.yml's `test` job: that was the only place
      # coverage reached codecov, and folding the file in without this step
      # would have removed the upload without anyone noticing.
      - uses: codecov/codecov-action@v3
        with:
          files: ./coverage/lcov.info
          flags: ${{ inputs.codecov-flag }}
          fail_ci_if_error: false

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-node-${{ inputs.service-name }}
          path: coverage/
          retention-days: 1

  coverage:
    runs-on: ${{ fromJSON(vars.KORZ_RUNNER_LABELS || '"ubuntu-latest"') }}
    needs: test
    if: always()
    timeout-minutes: 10
    outputs:
      # Exposed so the publish-status job below can read it. It used to be
      # consumed by a step in this same job, which is why it was never an output.
      percentage: ${{ steps.coverage.outputs.percentage }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: coverage-node-${{ inputs.service-name }}
          path: coverage/

      - name: Extract coverage percentage
        id: coverage
        run: |
          if [ -f "coverage/coverage-summary.json" ]; then
            COVERAGE=$(jq '.total.lines.pct' coverage/coverage-summary.json)
          else
            COVERAGE="0"
          fi
          echo "percentage=$COVERAGE" >> $GITHUB_OUTPUT
          echo "Coverage: $COVERAGE%"

  # A reusable WORKFLOW can only be called from a job, never from a step. This
  # lived inside `coverage` as a step, which makes the whole file invalid: the
  # run died at startup with 0 jobs, in 0 seconds, on every push since the file
  # was written -- 0 successes in the last 40 runs. Nothing consumed it on push,
  # so the red never reached anyone, and the repo had no gate of its own to say
  # otherwise.
  #
  # As its own job with `needs: coverage`, it is a legal workflow call and the
  # coverage number still flows through via job outputs.
  publish-status:
    needs: coverage
    # `master` instead of `develop`: default branches are split across the org
    # and develop is gone, so this gate was matching a branch no consumer has.
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
    uses: korz-sh/central-workflows/.github/workflows/publish-status.yml@main
    with:
      service-name: ${{ inputs.service-name }}
      coverage-percentage: ${{ needs.coverage.outputs.percentage }}
      run-id: ${{ github.run_id }}
    secrets: inherit
